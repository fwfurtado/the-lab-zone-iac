# yaml-language-server: $schema=https://atmos.tools/schemas/atmos/atmos-manifest/1.0/atmos-manifest.json

import:
  - catalog/_packer-lxc.yaml

components:
  packer:
    lxc:
      vars:
        app_name: caddy
        playbook_file: ansible/playbook-caddy.yml
        files:
          "/etc/caddy/Caddyfile":
            content: !include.raw stacks/catalog/packer/caddy/Caddyfile
            args: {}
