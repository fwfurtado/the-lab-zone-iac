# yaml-language-server: $schema=https://atmos.tools/schemas/atmos/atmos-manifest/1.0/atmos-manifest.json

import:
  - catalog/_vm.yaml

components:
  terraform:
    aistor-vm:
      metadata:
        component: patroni-vm
        terraform_workspace: "{{ .stack }}"
      vars:
        proxmox_node_name: !env PROXMOX_SSH_NODE_NAME
        proxmox_ssh_host: !env PROXMOX_SSH_HOST
        proxmox_ssh_username: !env PROXMOX_SSH_USERNAME
        ssh_public_keys:
          - !env SSH_PUBLIC_KEY
        vm_id: 1120
        vm_name: "aistor"
        vm_description: "MinIO AIStor - S3-compatible object storage"
        ip_cidr: "10.40.1.20/21"
        cpu: 4
        memory: 16384
        os_disk_size: 20
        tags:
          - "storage"
          - "minio"
          - "aistor"
